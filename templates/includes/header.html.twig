<header role="banner" id="tv-header" class="mb-6 sticky pb-2 top-0 inset-x-0 z-50 bg-primary">
    <div class="flex flex-col-reverse">

        <div id="tv-header__main" class="px-5">
            <div class="py-4 flex justify-between items-center gap-5 w-full max-w-prose mx-auto">
                {{ page.header }}
            </div>
        </div>

        <div class="tv-main-nav__wrapper h-0 overflow-hidden transition-all duration-300 ease-in-out bg-secondary">
        {{ page.primary_menu }}
        </div>

    </div>
</header>